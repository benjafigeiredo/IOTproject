"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const cli_lib_1 = require("@smartthings/cli-lib");
class DeviceDeleteCommand extends cli_lib_1.APICommand {
    async run() {
        const { args, argv, flags } = this.parse(DeviceDeleteCommand);
        await super.setup(args, argv, flags);
        const config = {
            primaryKeyName: 'deviceId',
            sortKeyName: 'name',
        };
        const id = await cli_lib_1.selectFromList(this, config, args.id, () => this.client.devices.list(), 'Select device to delete.');
        await this.client.devices.delete(id);
        this.log(`Device ${id} deleted.`);
    }
}
exports.default = DeviceDeleteCommand;
DeviceDeleteCommand.description = 'delete a device';
DeviceDeleteCommand.flags = cli_lib_1.APICommand.flags;
DeviceDeleteCommand.args = [{
        name: 'id',
        description: 'device UUID',
    }];
