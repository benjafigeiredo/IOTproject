export interface EventFormat {
    formatString: string;
    formatArgs?: string[];
    time: string;
}
export declare type EventFormatter = (event: any) => EventFormat;
export declare function logEvent(event: Event, formatter: EventFormatter): void;
