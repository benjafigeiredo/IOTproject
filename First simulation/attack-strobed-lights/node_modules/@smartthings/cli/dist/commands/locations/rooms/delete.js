"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const command_1 = require("@oclif/command");
const cli_lib_1 = require("@smartthings/cli-lib");
const rooms_1 = require("../rooms");
class RoomsDeleteCommand extends cli_lib_1.APICommand {
    async run() {
        const { args, argv, flags } = this.parse(RoomsDeleteCommand);
        await super.setup(args, argv, flags);
        const [roomId, locationId] = await rooms_1.chooseRoom(this, flags['location-id'], args.id);
        await this.client.rooms.delete(roomId, locationId);
        this.log(`room ${roomId} deleted`);
    }
}
exports.default = RoomsDeleteCommand;
RoomsDeleteCommand.description = 'delete a room';
RoomsDeleteCommand.flags = {
    ...cli_lib_1.APICommand.flags,
    'location-id': command_1.flags.string({
        char: 'l',
        description: 'a specific location to query',
    }),
};
RoomsDeleteCommand.args = [{
        name: 'id',
        description: 'room UUID',
    }];
RoomsDeleteCommand.aliases = ['rooms:delete'];
