"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const cli_lib_1 = require("@smartthings/cli-lib");
class LogoutCommand extends cli_lib_1.APICommand {
    async run() {
        const { args, argv, flags } = this.parse(LogoutCommand);
        await super.setup(args, argv, flags);
        if (this.token) {
            this.log(`Profile ${this.profileName} is set up using a PAT.`);
            this.exit();
        }
        this.authenticator.logout && await this.authenticator.logout();
        this.log('logged out');
    }
}
exports.default = LogoutCommand;
LogoutCommand.flags = cli_lib_1.APICommand.flags;
