import { AxiosRequestConfig } from 'axios';
import { SmartThingsURLProvider, Authenticator } from '@smartthings/core-sdk';
export interface ClientIdProvider extends SmartThingsURLProvider {
    clientId: string;
    baseOAuthInURL: string;
    oauthAuthTokenRefreshURL: string;
}
export declare const defaultClientIdProvider: ClientIdProvider;
export declare class LoginAuthenticator implements Authenticator {
    private profileName;
    private clientIdProvider;
    static init(credentialsFile: string): void;
    private clientId;
    private authenticationInfo?;
    private logger;
    constructor(profileName: string, clientIdProvider: ClientIdProvider);
    private base64URLEncode;
    private sha256;
    private delay;
    private readCredentialsFile;
    private writeCredentialsFile;
    private updateTokenFromResponse;
    login(): Promise<void>;
    logout(): Promise<void>;
    private refreshToken;
    authenticate(requestConfig: AxiosRequestConfig): Promise<AxiosRequestConfig>;
    authenticateGeneric(): Promise<string>;
}
