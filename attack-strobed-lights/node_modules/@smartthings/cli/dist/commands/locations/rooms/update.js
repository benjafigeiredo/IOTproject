"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const command_1 = require("@oclif/command");
const cli_lib_1 = require("@smartthings/cli-lib");
const rooms_1 = require("../rooms");
class RoomsUpdateCommand extends cli_lib_1.APICommand {
    async run() {
        const { args, argv, flags } = this.parse(RoomsUpdateCommand);
        await super.setup(args, argv, flags);
        const [roomId, locationId] = await rooms_1.chooseRoom(this, flags['location-id'], args.id);
        await cli_lib_1.inputAndOutputItem(this, { tableFieldDefinitions: rooms_1.tableFieldDefinitions }, (_, data) => this.client.rooms.update(roomId, data, locationId));
    }
}
exports.default = RoomsUpdateCommand;
RoomsUpdateCommand.description = 'update a room';
RoomsUpdateCommand.flags = {
    ...cli_lib_1.APICommand.flags,
    ...cli_lib_1.inputAndOutputItem.flags,
    'location-id': command_1.flags.string({
        char: 'l',
        description: 'a specific location to query',
    }),
};
RoomsUpdateCommand.args = [{
        name: 'id',
        description: 'room UUID',
    }];
RoomsUpdateCommand.aliases = ['rooms:update'];
