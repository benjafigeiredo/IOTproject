"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const command_1 = require("@oclif/command");
const cli_lib_1 = require("@smartthings/cli-lib");
const locations_1 = require("../../locations");
const rooms_1 = require("../rooms");
class RoomsCreateCommand extends cli_lib_1.APICommand {
    async run() {
        const { args, argv, flags } = this.parse(RoomsCreateCommand);
        await super.setup(args, argv, flags);
        const locationId = await locations_1.chooseLocation(this, flags['location-id']);
        await cli_lib_1.inputAndOutputItem(this, { tableFieldDefinitions: rooms_1.tableFieldDefinitions }, (_, location) => this.client.rooms.create(location, locationId));
    }
}
exports.default = RoomsCreateCommand;
RoomsCreateCommand.description = 'create a room';
RoomsCreateCommand.flags = {
    ...cli_lib_1.APICommand.flags,
    ...cli_lib_1.inputAndOutputItem.flags,
    'location-id': command_1.flags.string({
        char: 'l',
        description: 'a specific location to query',
    }),
};
RoomsCreateCommand.aliases = ['rooms:create'];
