"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const command_1 = require("@oclif/command");
const cli_lib_1 = require("@smartthings/cli-lib");
const apps_1 = require("../apps");
const aws_utils_1 = require("../../lib/aws-utils");
const common_flags_1 = require("../../lib/common-flags");
class AppUpdateCommand extends cli_lib_1.APICommand {
    async run() {
        const { args, argv, flags } = this.parse(AppUpdateCommand);
        await super.setup(args, argv, flags);
        const appId = await apps_1.chooseApp(this, args.id);
        const executeUpdate = async (_, data) => {
            if (flags.authorize) {
                if (data.lambdaSmartApp) {
                    if (data.lambdaSmartApp.functions) {
                        const requests = data.lambdaSmartApp.functions.map((it) => {
                            return aws_utils_1.addPermission(it, flags.principal, flags['statement-id']);
                        });
                        await Promise.all(requests);
                    }
                }
                else {
                    throw new Error('Authorization is not applicable to WebHook SmartApps');
                }
            }
            return this.client.apps.update(appId, data);
        };
        await cli_lib_1.inputAndOutputItem(this, { tableFieldDefinitions: apps_1.tableFieldDefinitions }, executeUpdate);
    }
}
exports.default = AppUpdateCommand;
AppUpdateCommand.description = 'update the OAuth settings of the app';
AppUpdateCommand.flags = {
    ...cli_lib_1.APICommand.flags,
    ...cli_lib_1.inputAndOutputItem.flags,
    authorize: command_1.flags.boolean({
        description: 'authorize Lambda functions to be called by SmartThings',
    }),
    ...common_flags_1.lambdaAuthFlags,
};
AppUpdateCommand.args = [{
        name: 'id',
        description: 'the app id',
    }];
