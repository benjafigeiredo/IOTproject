"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const cli_lib_1 = require("@smartthings/cli-lib");
const devices_1 = require("../devices");
class DeviceUpdateCommand extends cli_lib_1.APICommand {
    async run() {
        const { args, argv, flags } = this.parse(DeviceUpdateCommand);
        await super.setup(args, argv, flags);
        const id = await devices_1.chooseDevice(this, args.id);
        await cli_lib_1.inputAndOutputItem(this, { buildTableOutput: data => devices_1.buildTableOutput(this.tableGenerator, data) }, (_, data) => this.client.devices.update(id, data));
    }
}
exports.default = DeviceUpdateCommand;
DeviceUpdateCommand.description = "get the current status of all of a device's component's attributes";
DeviceUpdateCommand.flags = {
    ...cli_lib_1.APICommand.flags,
    ...cli_lib_1.inputAndOutputItem.flags,
};
DeviceUpdateCommand.args = [
    {
        name: 'id',
        description: 'the device id',
    },
];
