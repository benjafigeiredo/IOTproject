"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const command_1 = require("@oclif/command");
const cli_lib_1 = require("@smartthings/cli-lib");
const locations_1 = require("../locations");
const rules_1 = require("../rules");
class RulesCreateCommand extends cli_lib_1.APICommand {
    async run() {
        const { args, argv, flags } = this.parse(RulesCreateCommand);
        await super.setup(args, argv, flags);
        const locationId = await locations_1.chooseLocation(this, flags['location-id']);
        await cli_lib_1.inputAndOutputItem(this, { tableFieldDefinitions: rules_1.tableFieldDefinitions }, (_, rule) => this.client.rules.create(rule, locationId));
    }
}
exports.default = RulesCreateCommand;
RulesCreateCommand.description = 'create a rule';
RulesCreateCommand.flags = {
    ...cli_lib_1.APICommand.flags,
    ...cli_lib_1.inputAndOutputItem.flags,
    'location-id': command_1.flags.string({
        char: 'l',
        description: 'a specific location to query',
    }),
};
