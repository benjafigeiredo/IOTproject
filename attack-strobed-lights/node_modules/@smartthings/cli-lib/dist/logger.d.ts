import { Configuration as LoggingConfig } from 'log4js';
import { Logger as APILogger } from '@smartthings/core-sdk';
export declare class LogManager {
    private getLog4jsLogger?;
    private loggersByName;
    constructor();
    init(config: LoggingConfig): void;
    getLogger(name: string): APILogger;
}
export declare function defaultLoggingConfig(logFilename: string): LoggingConfig;
export declare function loadLoggingConfig(configFilename: string, defaultConfig: LoggingConfig): LoggingConfig;
export declare const logManager: LogManager;
