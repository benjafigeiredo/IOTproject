import { flags } from '@oclif/command';
import { Authenticator, SmartThingsClient } from '@smartthings/core-sdk';
import { SmartThingsCommand } from './smartthings-command';
/**
 * Base class for commands that need to use Rest API commands via the
 * SmartThings Core SDK.
 */
export declare abstract class APICommand extends SmartThingsCommand {
    static flags: {
        token: flags.IOptionFlag<string | undefined>;
        language: flags.IOptionFlag<string | undefined>;
        help: import("@oclif/parser/lib/flags").IBooleanFlag<void>;
        profile: flags.IOptionFlag<string>;
    };
    protected token?: string;
    private _authenticator?;
    get authenticator(): Authenticator;
    protected clientIdProvider: import("./login-authenticator").ClientIdProvider;
    private _client?;
    get client(): SmartThingsClient;
    setup(args: {
        [name: string]: any;
    }, argv: string[], flags: {
        [name: string]: any;
    }): Promise<void>;
}
