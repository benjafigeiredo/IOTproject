"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const cli_lib_1 = require("@smartthings/cli-lib");
const channels_1 = require("../channels");
const edge_command_1 = require("../../../lib/edge-command");
class ChannelsUpdateCommand extends edge_command_1.EdgeCommand {
    async run() {
        const { args, argv, flags } = this.parse(ChannelsUpdateCommand);
        await super.setup(args, argv, flags);
        const id = await channels_1.chooseChannel(this, 'Choose a channel to patch.', args.id);
        await cli_lib_1.inputAndOutputItem(this, { tableFieldDefinitions: channels_1.tableFieldDefinitions }, (_, channelMods) => this.edgeClient.channels.update(id, channelMods));
    }
}
exports.default = ChannelsUpdateCommand;
ChannelsUpdateCommand.description = 'update a channel';
ChannelsUpdateCommand.flags = {
    ...edge_command_1.EdgeCommand.flags,
    ...cli_lib_1.inputAndOutputItem.flags,
};
ChannelsUpdateCommand.args = [{
        name: 'id',
        description: 'the channel id',
    }];
