"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const cli_lib_1 = require("@smartthings/cli-lib");
const devices_1 = require("../devices");
class DeviceHealthCommand extends cli_lib_1.APICommand {
    async run() {
        const { args, argv, flags } = this.parse(DeviceHealthCommand);
        await super.setup(args, argv, flags);
        const deviceId = await devices_1.chooseDevice(this, args.id, { allowIndex: true });
        const health = await this.client.devices.getHealth(deviceId);
        await cli_lib_1.formatAndWriteItem(this, { tableFieldDefinitions: ['deviceId', 'state', 'lastUpdatedDate'] }, health);
    }
}
exports.default = DeviceHealthCommand;
DeviceHealthCommand.description = 'get the current health status of a device';
DeviceHealthCommand.flags = {
    ...cli_lib_1.APICommand.flags,
    ...cli_lib_1.formatAndWriteItem.flags,
};
DeviceHealthCommand.args = [{
        name: 'id',
        description: 'the device id',
    }];
